export class Site {
  /**
   * The name of the site.
   */
  name: string;
  /**
   * Whether the site can escalate.
   */
  isEscalation: boolean;
  /**
   * Whether the sites can escalate.
   */
  canEscalate: boolean;
}